[package]
name = "swap-venue"
version = { workspace = true }
edition = { workspace = true }
publish = false

[lib]
crate-type = ["rlib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }
