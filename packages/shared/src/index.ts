export * from "./types.js";
// Generated contract bindings (foundation §1). Also exposed via the `./bindings` subpath
// (`@shadowkit/shared/bindings`) per the package `exports` map; re-exported here as a namespace so
// `@shadowkit/shared` consumers can reach the typed clients without the subpath collision risk.
export * as bindings from "./bindings/index.js";
