---
// Dark-only docs: ShadowKit's "Anonymity Set" system has no light mode (see docs/DESIGN.md).
// Overriding Starlight's ThemeSelect with an empty component removes the light/dark toggle.
// The dark palette is forced on :root in src/styles/starlight.css.
---
